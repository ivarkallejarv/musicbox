export class WebAudioFontPlayer {
  adjustPreset(context: AudioContext, preset: { zones: any[] }) {}
  queueWaveTable(
    context: AudioContext,
    destination: AudioDestinationNode,
    preset: { zones: any[] },
    time: number,
    pitch: number,
    duration: number,
  ) {}
}
